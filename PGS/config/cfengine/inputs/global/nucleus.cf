
control:
	RPMInstallCommand 	= ( "/usr/bin/yum -y --enablerepo=epel -d 0 -e 0 install %s " )
	actionsequence		= ( packages )
	

packages:

	numpy		pkgmgr=rpm cmp=ge version=0:1.2.1 action=install
	python-matplotlib		pkgmgr=rpm cmp=ge version=0:0.99.1.2 action=install
