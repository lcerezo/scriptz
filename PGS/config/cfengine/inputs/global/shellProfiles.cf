control:

	actionsequence = ( copy )
	

copy:

	linux::
			${configroot}/config/cfengine/inputs/global/cfgfiles/skel-cshrc
							dest=/etc/skel/.cshrc
							mode=644
							action=fix
							server=${server}
							backup=false
							type=sum
							inform=false
