

	control:
		actionsequence = ( processes )


	processes:
		
		cfservers::
				"cfservd" restart "/sbin/service cfservd restart"
		linux::
				"cfenvd" restart "/sbin/service cfenvd restart"
				"cfexecd" restart "/sbin/service cfexecd restart"
						
