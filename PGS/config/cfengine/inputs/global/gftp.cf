control:

        actionsequence  = ( packages shellcommands )

packages:
        AllLinuxWorkstations::

                gftp  pkgmgr=rpm elsedefine=needs_gftp

shellcommands:

        needs_gftp::

                "${yum_bin} -y install gftp" 
				ifelapsed=900	
