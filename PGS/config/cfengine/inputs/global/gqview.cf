	control:

		actionsequence = ( packages shellcommands )

	
	packages:

		gqview pkgmgr=rpm elsedefine=needs_gqview

	shellcommands:

		!servers.needs_gqview::
        	        "${yum_bin} -y install gqview" ifelapsed=300 
						expireafter=3
	
