control:

        actionsequence  = ( packages shellcommands )

packages:

        	vim-X11  	pkgmgr=rpm 
				cmp=ge
				version=0:7.0.109
				elsedefine=needs_gvim

shellcommands:

        needs_gvim::
        
		        "${yum_bin} -y install vim-X11"
