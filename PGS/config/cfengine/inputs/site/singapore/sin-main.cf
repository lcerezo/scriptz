
import:

	site/$(site)/autofs.cf

disable:
	/etc/init.d/nodes_nfsmnt.post
	/etc/yum.repos.d/singpgs-CentOS5.repo

editfiles:
	
	{ /etc/dhclient-eth0.conf
		DeleteLinesContaining "send host-name"
		CommentLinesMatching  "supersede domain-name"
	}
links:

	/datadb ->! /research/datadb
	/cm	->! /research/cm
	/cfs	->! /research/cfs
