control:
	
	actionsequence	= ( packages shellcommands files )
	Split		= ( | )

	linux::
	ICA_rpm = ( ICAClient-9.0-1.i386.rpm )

packages:
	
	ICAClient			version=0:9.0-1
				cmp=ge
				pkgmgr=rpm
				elsedefine=needs_ICA

shellcommands:

	needs_ICA::
		"$(rpm_bin) -ivh $(rpms_base_url)$(ICA_rpm)"
